# Money Map Manager - Environment Variables
# Copy this file to .env and fill in your values.
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your ANTHROPIC_API_KEY
#   docker compose up -d

# ============================================
# Required
# ============================================

# Anthropic API key for Claude categorization (required).
# Get yours at: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-xxxxx

# ============================================
# Optional
# ============================================

# Anthropic API base URL (optional, for proxies or alternative endpoints).
# For Docker: use host.docker.internal instead of localhost to reach host services.
# ANTHROPIC_BASE_URL=https://api.anthropic.com
# ANTHROPIC_BASE_URL=http://host.docker.internal:8317  # Example for local proxy in Docker

# Extended thinking mode (optional, defaults to false)
# Enables Claude's extended thinking for deeper reasoning on complex tasks
# ANTHROPIC_THINKING_ENABLED=false

# Token budget for thinking (optional, defaults to 10000)
# Higher values allow more reasoning but increase cost and latency
# ANTHROPIC_THINKING_BUDGET=10000

# Claude model to use (optional, defaults to claude-sonnet-4-20250514).
# ANTHROPIC_MODEL=claude-sonnet-4-20250514

# ============================================
# Docker-Specific (usually no changes needed)
# ============================================

# These are set automatically in docker-compose.yml:
# - DATABASE_URL=sqlite:///./data/moneymap.db
# - FRONTEND_URL=http://localhost:4000
# - BACKEND_PORT=8000

# For non-localhost deployments (VPS, cloud), set the public API URL.
# This is baked into the frontend build for client-side API calls.
# NEXT_PUBLIC_API_URL=https://your-domain.com:8000
